export interface SseSendResponse {
  lastEventId: string;
  title: string;
  content: string;
  name: string;
}

export interface SseSubscribeResponse {
  lastEventId: string;
  title: string;
  content: string;
  name: string;
}
